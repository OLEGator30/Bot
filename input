{
	@begin:
		print "Month #", ?turn();
		buy(2,?raw_price());
		sell(?production(),?production_price());
		int $topprod=2;
		if (?raw()<$topprod)
			$topprod=?raw()
		prod($topprod);
		endturn;
		goto @begin;
}
